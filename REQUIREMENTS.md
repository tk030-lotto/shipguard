# shipguard 要件定義書

## 1. プロジェクト概要

* **名称**: `shipguard`
* **目的**: 個人開発者が本番ローンチ直前に抱える「シークレットの漏洩」「DBセキュリティの穴」「環境変数の不整合」に対する不安を解消し、自信を持ってデプロイできる状態をローカル完結で作る。
* **運用方針**: サーバー運用費・外部サービス費用0円のローカル完結型CLI工具。ユーザーのローカル環境（またはCI環境）で完全に動作し、アカウント登録不要・外部API通信なし・テレメトリなし・クラウド保存なしを徹底する。

## 2. ターゲットと課題

* **対象**: Next.js / Supabase / Cloudflare / Firebase 等を利用してWebアプリを開発する個人開発者。
* **解決する課題**:
  * `.env` の秘匿キーがフロントエンド用バンドル（`NEXT_PUBLIC_` 等）に混入する事故。
  * Supabase等のRLS（Row Level Security）が未有効化のまま公開されるリスク。
  * CORSの `Access-Control-Allow-Origin: *` 設定放置。
  * 監査結果や過去の検証履歴（ログ）がローカルに残っておらず、再検証や差分把握が困難な問題。

## 3. 機能要件

* **F-1: 静的ファイルスキャン機能**
  * プロジェクトルートから対象拡張子（`.ts`, `.tsx`, `.js`, `.env*`, `.sql` 等）を再帰的に走査。
  * `.gitignore` およびデフォルトの除外リスト（`node_modules`, `.next`, `dist` 等）を自動適用。

* **F-2: 多層ルールベース監査エンジン**
  * **Secret Leak (SEC-001)**: ハードコードされた秘密鍵（Stripe, OpenAI, AWS, 汎用Private Keyなど）の検知。
  * **Env Misconfiguration (SEC-002, CFG-001)**: `.env.example` と `.env` の差分検知、クライアント公開プレフィックスの誤用検知。
  * **DB Security (SEC-003)**:
    * 単一ファイル完結判定ではなく、プロジェクト内の全マイグレーションSQLファイルを突合して `CREATE TABLE` に対する `ENABLE ROW LEVEL SECURITY` の追従判定を実施。
    * 設定ファイル（`.shipguardrc.json`）による除外テーブル指定に対応し、誤検知を低減。
  * **Network Security (SEC-004)**: サーバー側ルートでのワイルドカードCORS設定、危険なHTTPヘッダー設定の検知。

* **F-3: 監査ログ永続化機能**
  * 監査実行日時、対象コミットハッシュ、検知数、判定結果を `.shipguard/audit.log`（NDJSON形式）に追記保存。外部DBを用いずにローカルで履歴性を持たせる。

* **F-4: レポート出力機能**
  * コンソール（CLI）での視認性の高いカラー表示。
  * オプションでMarkdown形式（`audit-report.md`）およびJSON形式での出力。

## 4. 非機能要件

* **保守性・運用コスト**: 外部サーバーやAPIバックエンドを持たない完全スタンドアロン構成。サーバー運用費・外部サービス費用0円、ローカル完結。
* **パフォーマンス**: 5,000ファイル規模のリポジトリを3秒以内に走査完了すること。
* **プライバシー・セキュリティ**: ソースコードや設定値を外部ネットワークに一切送信しない（オフライン動作保証、テレメトリ完全排除）。
* **互換性**: Node.js v18以上、Bun、Denoで動作すること。